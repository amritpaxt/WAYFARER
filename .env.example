JWT_SECRET=replace-with-a-long-random-secret
DEMO_MODE=false
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Use a persistent disk/volume path in production, e.g. /var/data/wayfarer.db
DATABASE_PATH=wayfarer.db
